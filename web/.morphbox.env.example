# MorphBox Environment Configuration
# Copy this file to .morphbox.env and customize as needed

# Network Binding Mode
# Options: local (default), external, vpn
MORPHBOX_BIND_MODE=local

# Custom host binding (optional)
# Leave commented to use auto-detection based on bind mode
# MORPHBOX_HOST=0.0.0.0

# Docker Container SSH Password
# SECURITY: Change this password in production!
# This password is used for SSH access to the Docker container
MORPHBOX_VM_PASSWORD=changeme

# Authentication Settings (for external/vpn access)
# Leave blank to auto-generate on startup
# MORPHBOX_AUTH_USERNAME=admin
# MORPHBOX_AUTH_PASSWORD=

# WebSocket Configuration
# MORPHBOX_VM_HOST=localhost
# MORPHBOX_VM_PORT=2222
# MORPHBOX_VM_USER=morphbox

# Allowed Origins for WebSocket connections (comma-separated)
# Default: http://localhost:8008,http://localhost:8009
# MORPHBOX_ALLOWED_ORIGINS=http://localhost:8008,http://localhost:8009

# Claude API Key (optional - can be set in container)
# ANTHROPIC_API_KEY=your-api-key-here

# Development Settings
# Set to true to skip security warnings
# MORPHBOX_DEV_MODE=false