# MorphBox Configuration Example
# Copy this file to .morphbox.env to set default configuration

# Network binding mode: "local" or "external"
# WARNING: "external" exposes MorphBox to your network
# MORPHBOX_BIND_MODE=local

# Custom ports (optional)
# WEB_PORT=8008
# WS_PORT=8009

# Host binding (advanced - use MORPHBOX_BIND_MODE instead)
# MORPHBOX_HOST=localhost